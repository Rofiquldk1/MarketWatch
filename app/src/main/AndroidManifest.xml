<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marketwatchsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.CCIEDeptActivity" />
        <activity android:name=".activity.NBRDeptActivity" />
        <activity android:name=".activity.TCBDeptActivity" />
        <activity android:name=".activity.AgriculturalDeptActivity" />
        <activity android:name=".activity.SelectDepartmentActivity" />
        <activity android:name=".activity.StatisticActivity" />
        <activity android:name=".activity.ActionActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.PhoneLoginActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.DistributeInfoActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>